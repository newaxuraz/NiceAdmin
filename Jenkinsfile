pipeline {
    agent any

    stages {
        stage('git checkuot sample') {
            steps {
                git branch: 'main', url: 'https://github.com/newaxuraz/NiceAdmin'
            }
        }
         
    }
}
